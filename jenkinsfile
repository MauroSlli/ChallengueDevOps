pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Clona el repositorio desde Git
                git 'https://github.com/MauroSlli/ChallengueDevOps.git'
            }
        }
    }
}

